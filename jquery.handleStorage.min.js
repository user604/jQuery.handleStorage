/**
 *
 * jQuery plugin to impliment local storage with optional AES
 * encryption support via the Gibberish-AES libraries
 *
 * Fork me @ https://www.github.com/jas-/jQuery.handleStorage
 *
 * FEATURES:
 * - HTML5 localStorage support
 * - HTML5 sessionStorage support
 * - Cookie support
 * - AES encryption support
 *
 * REQUIREMENTS:
 * - jQuery libraries (required - http://www.jquery.com)
 * - jQuery cookie plugin (optional - http://plugins.jquery.com/files/jquery.cookie.js.txt)
 * - Gibberish-AES libraries (optional - https://github.com/mdp/gibberish-aes)
 *
 * OPTIONS:
 * - appID:   Unique identifier used as storage object key
 * - storage: HTML5 localStorage, sessionStorage and cookies supported
 * - aes:     Use AES encryption for local storage
 * - uuid:    Random RFC-4122 string used as AES password
 *
 * Author: Jason Gerfen
 * Email: jason.gerfen@gmail.com
 * Copyright: Jason Gerfen
 *
 * License: GPL
 *
 */

/*
(function($){
 $.fn.handleStorage = function(method) {
  var defaults = {
   appID:   'jQuery.handleStorage',
   storage: 'localStorage',
   aes:     false,
   uuid:    '',
   form:    $(this).attr('id'),
   data:    {}
  };
  var methods = {
   init: function(options){
    var opts = $.extend({}, defaults, options);
    if (validateOptions(opts)){
     handleKey(opts);
     opts.data[opts.appID] = (existing(opts)) ? existing(opts) : {};
     var orig = getStorage(opts);
     if ((typeof orig==='object')&&(sizeChk(orig)>0)){
      setForm(opts, orig);
     }
     $('#'+opts.form).live('submit', function(e){
      saveForm(opts);
     });
     return true;
    } else {
     return false;
    }
   }
  };
  var sizeChk = function(obj) {
   var n = 0;
   $.each(obj, function(k, v){
    if (obj.hasOwnProperty(k)) n++;
   });
   return n;
  }
  var setItem = function(type, k, v) {
   var x = false;
   type = (validateStorage(type)) ? type : 'cookie';
   switch(type) {
    case 'localStorage':
     x = setLocal(k, v);
     break;
    case 'sessionStorage':
     x = setSession(k, v);
     break;
    case 'cookie':
     x = setCookie(k, v);
     break;
    default:
     x = setLocal(k, v);
     break;
   }
   return x;
  }
  var getItem = function(type, k) {
   var x = false;
   type = (validateStorage(type)) ? type : 'cookie';
   switch(type) {
    case 'localStorage':
     x = getLocal(k);
     break;
    case 'sessionStorage':
     x = getSession(k);
     break;
    case 'cookie':
     x = getCookie(k);
     break;
    default:
     x = getLocal(k);
     break;
   }
   return x;
  }
  var setLocal = function(k, v) {
   return (localStorage.setItem(k, v)) ? false : true;
  }
  var setSession = function(k, v) {
   return (sessionStorage.setItem(k, v)) ? false : true;
  }
  var setCookie = function(k, v) {
   if (typeof $.cookie === 'function') {
    return ($.cookie(k, v, {expires: 7})) ? true : false;
   } else {
    return false;
   }
  }
  var getLocal = function(k) {
   return (localStorage.getItem(k)) ? localStorage.getItem(k) : false;
  }
  var getSession = function(k) {
   return (sessionStorage.getItem(k)) ? sessionStorage.getItem(k) : false;
  }
  var getCookie = function(name) {
   if (typeof $.cookie === 'function') {
    return ($.cookie(name)) ? $.cookie(name) : false;
   } else {
    return false;
   }
  }
  var existing = function(options) {
   return (getItem(options.storage, options.appID)) ?
    JSON.parse(getItem(options.storage, options.appID)) : false;
  }
  var getStorage = function(options) {
   var ret={}, x;
   if (validateString(options.data[options.appID][options.form])){
    $.each($('#'+options.form+' :text, :password, :file, input:hidden, input:checkbox:checked, input:radio:checked, textarea, input[type="email"], input[type="url"], input[type="number"], input[type="range"], input[type="date"], input[type="month"], input[type="week"], input[type="time"], input[type="datetime"], input[type="datetime-local"], input[type="search"], input[type="color"]'), function(k, v){
     if ((validateString(v.name)!==false)&&
         (validateString(options.data[options.appID][options.form][v.name])!==false)){
      ret[v.name] = ((options.aes)&&(options.key)&&(x!==false)) ?
       GibberishAES.dec(options.data[options.appID][options.form][v.name], options.uuid) :
       options.data[options.appID][options.form][v.name];
     }
    });
   }
   return ret;
  }
  var setForm = function(options, arg){
   $.each(arg, function(a, b){
    if (($('#'+options.form+' input[name='+a+']').attr('name')===a)&&
        (validateString(b)!==false)){
     $('#'+options.form+' input[name='+a+']').val(b);
    }
   });
  }
  var saveForm = function(options) {
   var x={}; x[options.form]={};
   $.each($('#'+options.form+' :text, :password, :file, input:hidden, input:checkbox:checked, input:radio:checked, textarea, input[type="email"], input[type="url"], input[type="number"], input[type="range"], input[type="date"], input[type="month"], input[type="week"], input[type="time"], input[type="datetime"], input[type="datetime-local"], input[type="search"], input[type="color"]'), function(k, v){
    if ((validateString(v.value)!==false)&&(validateString(v.name)!==false)){
     x[options.form][v.name] = ((options.aes)&&(options.key)) ?
      GibberishAES.enc(v.value, options.uuid) : v.value;
    }
   });
   options.data[options.appID] = (sizeChk(options.data[options.appID])>0) ?
    $.extend({}, options.data[options.appID], x) : x;
   setItem(options.storage, options.appID, JSON.stringify(options.data[options.appID]));
  }
  var validateString = function(string){
   if (string){
    return ((string===false)||(string.length===0)||(!string)||(string===null)||
            (string==='')||(typeof string==='undefined')) ? false : true;
   } else {
    return false;
   }
  }
  var validateStorage = function(type){
   try {
    return ((type in window)&&(window[type])) ? true : false;
   } catch (e) {
    return false;
   }
  }
  var validateOptions = function(opts){
   var ret = true;
   if (opts.aes){
    if (!$.isFunction(GibberishAES.enc)){
     console.log('AES use specified but required libraries not available.'+
                 'Please include the Gibberish-AES libs...');
     ret = false;
    }
   }
   if (opts.storage==='cookie'){
    if (!$.isFunction($.cookie)){
     console.log('Cookie use specified but required libraries not available.'+
                 'Please include the jQuery cookie plugin...');
     ret = false;
    }
   }
   return ret;
  }
  var genUUID = function(len){
   var chars = '0123456789abcdef'.split('');
   var uuid = [], rnd = Math.random, r;
   uuid[8] = uuid[13] = uuid[18] = uuid[23] = '-';
   uuid[14] = '4';
   for (var i = 0; i < 36; i++){
    if (!uuid[i]){
     r = 0 | rnd()*16;
     uuid[i] = chars[(i == 19) ? (r & 0x3) | 0x8 : r & 0xf];
    }
   }
   return (len!==null) ?
    uuid.join('').replace(/-/g, '').split('',len).join('') : uuid.join('');
  }
  var handleKey = function(options) {
   if (options.aes) {
    options.key = (getItem(options.storage, 'uuid')) ?
     getItem(options.storage, 'uuid') : genUUID(null);
    setItem(options.storage, 'uuid', options.key);
   }
  }
  var __recurse = function(obj){
   $.each(obj, function(x,y){
    if (typeof y==='object'){
     __recurse(y);
    } else {
     console.log(x+' => '+y);
    }
   });
  }
  if (methods[method]){
   return methods[method].apply(this, Array.prototype.slice.call(arguments, 1));
  } else if ((typeof method==='object')||(!method)){
   return methods.init.apply(this, arguments);
  } else {
   $.error('Method '+method+' does not exist on '+opts.name);
  }
 };
})(jQuery);
*/

(function(a){a.fn.handleStorage=function(g){var k={appID:"jQuery.handleStorage",storage:"localStorage",aes:false,uuid:"",form:a(this).attr("id"),data:{}};var v={init:function(x){var y=a.extend({},k,x);if(h(y)){m(y);y.data[y.appID]=(o(y))?o(y):{};var z=b(y);if((typeof z==="object")&&(l(z)>0)){i(y,z)}a("#"+y.form).live("submit",function(A){t(y)});return true}else{return false}}};var l=function(x){var y=0;a.each(x,function(A,z){if(x.hasOwnProperty(A)){y++}});return y};var e=function(B,A,z){var y=false;B=(w(B))?B:"cookie";switch(B){case"localStorage":y=c(A,z);break;case"sessionStorage":y=f(A,z);break;case"cookie":y=n(A,z);break;default:y=c(A,z);break}return y};var j=function(A,z){var y=false;A=(w(A))?A:"cookie";switch(A){case"localStorage":y=d(z);break;case"sessionStorage":y=s(z);break;case"cookie":y=u(z);break;default:y=d(z);break}return y};var c=function(y,x){return(localStorage.setItem(y,x))?false:true};var f=function(y,x){return(sessionStorage.setItem(y,x))?false:true};var n=function(y,x){if(typeof a.cookie==="function"){return(a.cookie(y,x,{expires:7}))?true:false}else{return false}};var d=function(x){return(localStorage.getItem(x))?localStorage.getItem(x):false};var s=function(x){return(sessionStorage.getItem(x))?sessionStorage.getItem(x):false};var u=function(x){if(typeof a.cookie==="function"){return(a.cookie(x))?a.cookie(x):false}else{return false}};var o=function(x){return(j(x.storage,x.appID))?JSON.parse(j(x.storage,x.appID)):false};var b=function(A){var z={},y;if(p(A.data[A.appID][A.form])){a.each(a("#"+A.form+' :text, :password, :file, input:hidden, input:checkbox:checked, input:radio:checked, textarea, input[type="email"], input[type="url"], input[type="number"], input[type="range"], input[type="date"], input[type="month"], input[type="week"], input[type="time"], input[type="datetime"], input[type="datetime-local"], input[type="search"], input[type="color"]'),function(B,x){if((p(x.name)!==false)&&(p(A.data[A.appID][A.form][x.name])!==false)){z[x.name]=((A.aes)&&(A.key)&&(y!==false))?GibberishAES.dec(A.data[A.appID][A.form][x.name],A.uuid):A.data[A.appID][A.form][x.name]}})}return z};var i=function(y,x){a.each(x,function(A,z){if((a("#"+y.form+" input[name="+A+"]").attr("name")===A)&&(p(z)!==false)){a("#"+y.form+" input[name="+A+"]").val(z)}})};var t=function(z){var y={};y[z.form]={};a.each(a("#"+z.form+' :text, :password, :file, input:hidden, input:checkbox:checked, input:radio:checked, textarea, input[type="email"], input[type="url"], input[type="number"], input[type="range"], input[type="date"], input[type="month"], input[type="week"], input[type="time"], input[type="datetime"], input[type="datetime-local"], input[type="search"], input[type="color"]'),function(A,x){if((p(x.value)!==false)&&(p(x.name)!==false)){y[z.form][x.name]=((z.aes)&&(z.key))?GibberishAES.enc(x.value,z.uuid):x.value}});z.data[z.appID]=(l(z.data[z.appID])>0)?a.extend({},z.data[z.appID],y):y;e(z.storage,z.appID,JSON.stringify(z.data[z.appID]))};var p=function(x){if(x){return((x===false)||(x.length===0)||(!x)||(x===null)||(x==="")||(typeof x==="undefined"))?false:true}else{return false}};var w=function(x){try{return((x in window)&&(window[x]))?true:false}catch(y){return false}};var h=function(y){var x=true;if(y.aes){if(!a.isFunction(GibberishAES.enc)){console.log("AES use specified but required libraries not available.Please include the Gibberish-AES libs...");x=false}}if(y.storage==="cookie"){if(!a.isFunction(a.cookie)){console.log("Cookie use specified but required libraries not available.Please include the jQuery cookie plugin...");x=false}}return x};var q=function(x){var C="0123456789abcdef".split("");var A=[],z=Math.random,B;A[8]=A[13]=A[18]=A[23]="-";A[14]="4";for(var y=0;y<36;y++){if(!A[y]){B=0|z()*16;A[y]=C[(y==19)?(B&3)|8:B&15]}}return(x!==null)?A.join("").replace(/-/g,"").split("",x).join(""):A.join("")};var m=function(x){if(x.aes){x.key=(j(x.storage,"uuid"))?j(x.storage,"uuid"):q(null);e(x.storage,"uuid",x.key)}};var r=function(x){a.each(x,function(z,A){if(typeof A==="object"){r(A)}else{console.log(z+" => "+A)}})};if(v[g]){return v[g].apply(this,Array.prototype.slice.call(arguments,1))}else{if((typeof g==="object")||(!g)){return v.init.apply(this,arguments)}else{a.error("Method "+g+" does not exist on "+opts.name)}}}})(jQuery);
